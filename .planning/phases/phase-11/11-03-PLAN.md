---
phase: phase-11
plan: 11-03
type: execute
wave: 2
depends_on: [11-02]
must_haves:
  truths:
    - Agents must receive the full user profile for personalization.
    - Isolation must be verified across multiple concurrent users.
  artifacts:
    - path: src/controllers/chat.py
      provides: Personalized user_context for LangGraph.
      min_lines: 20
    - path: tests/api/test_isolation.py
      provides: Multi-user cross-access tests.
      min_lines: 50
  key_links:
    - from: Phase 11
      to: REQ-062
      via: Personalized agent context and thread ownership.
files_modified:
  - src/controllers/chat.py
  - tests/api/test_isolation.py
  - tests/api/test_portfolio.py
  - tests/api/test_transactions.py
  - tests/api/test_threads.py
autonomous: true
requirements:
  - REQ-062: Thread Ownership
---

# 11-03: LangGraph Profile Injection & Multi-User Verification

Personalize the AI agent experience and prove that isolation is impenetrable across multiple concurrent users.

## Tasks

<task id="1">
   <name>Personalize LangGraph Context</name>
   <files>
     - src/controllers/chat.py
   </files>
   <action>
     - Update the `initial_state` construction in `src/controllers/chat.py`.
     - Fetch the authenticated user's profile: `first_name`, `risk_tolerance`, `base_currency`.
     - Inject this data into `user_context` within the LangGraph state.
   </action>
   <verify>
     - Verify agents receive personalized context via logs or debug outputs.
   </verify>
   <done>
     - [ ] LangGraph context personalized.
   </done>
</task>

<task id="2">
   <name>Refactor Existing Tests for Auth</name>
   <files>
     - tests/api/test_portfolio.py
     - tests/api/test_transactions.py
     - tests/api/test_threads.py
   </files>
   <action>
     - Update all existing API tests to include a valid JWT in the `Authorization` header.
     - Mock the `get_current_user` dependency or use a real test user from the DB.
   </action>
   <verify>
     - `pytest tests/api/` should pass all existing functionality tests.
   </verify>
   <done>
     - [ ] Existing tests refactored for JWT.
   </done>
</task>

<task id="3">
   <name>Implement Multi-User Isolation Tests</name>
   <files>
     - tests/api/test_isolation.py
   </files>
   <action>
     - Create `tests/api/test_isolation.py`.
     - Implement scenarios:
       - User A creates a transaction; User B attempts to GET it -> 404.
       - User A creates a thread; User B attempts to POST a message to it -> 404.
       - User A deletes a thread; User B attempts to DELETE it -> 404.
       - Verify User C (new) gets a 200 OK with zeroed data for portfolio.
   </action>
   <verify>
     - `pytest tests/api/test_isolation.py` passes.
   </verify>
   <done>
     - [ ] Multi-user isolation verified.
   </done>
</task>

## Success Criteria
- Agents personalize suggestions based on user risk tolerance and currency.
- No cross-user data leakage possible via API.
- All system tests pass with JWT authentication.
