---
phase: phase-14
plan: 14-03
type: execute
wave: 3
depends_on: [14-02]
must_haves:
  truths:
    - Deprecated custom SSE schemas must be removed to avoid confusion.
    - History retrieval must conform to the new protocol format.
  artifacts:
    - path: src/controllers/threads.py
      provides: Protocol-compliant history retrieval.
      min_lines: 40
  key_links:
    - from: Phase 14
      to: Phase 14 Cleanup
      via: Deletion of legacy artifacts.
files_modified:
  - src/controllers/threads.py
  - src/controllers/chat.py
  - src/schemas/chat.py
autonomous: true
requirements:
  - Phase 14 Goal: Protocol Compliance & Cleanup
---

# 14-03: History Backfill & Legacy Cleanup

Align history playback with the new official protocol and decommission all custom legacy streaming logic.

## Tasks

<task id="1">
   <name>Update History Protocol</name>
   <files>
     - src/controllers/threads.py
   </files>
   <action>
     - Refactor `GET /threads/{id}/history` to yield messages using the `event: messages` format instead of a plain JSON response.
     - This ensures the Dart client can use the same parsing logic for both live runs and history playback.
     - Note: This might change the return type to a Stream if desired, or we keep it as JSON but update the payload structure to match standard LangGraph message objects.
     - *Decision:* Keep it as a JSON response for history (standard REST), but update the payload to be a list of standard LangGraph message dictionaries.
   </action>
   <verify>
     - Verify history payload matches the structure of `event: messages` data.
   </verify>
   <done>
     - [ ] History payload updated.
   </done>
</task>

<task id="2">
   <name>Decommission Legacy Code</name>
   <files>
     - src/controllers/chat.py
     - src/schemas/chat.py
   </files>
   <action>
     - Delete `src/controllers/chat.py` (the entire `/chat` endpoint).
     - Remove `ChatRequest`, `ChatTokenEvent`, `ChatStatusEvent`, `ChatErrorEvent` from `src/schemas/chat.py`.
     - Update `main.py` to remove the `chat_router` inclusion.
   </action>
   <verify>
     - `grep -r "/chat" .` should return no active endpoint definitions.
     - Application starts and passes all Phase 14 tests without the old code.
   </verify>
   <done>
     - [ ] Legacy code purged.
   </done>
</task>

## Success Criteria
- History playback uses standard LangGraph message schemas.
- Legacy custom SSE code is completely removed.
- All integration tests for the new protocol pass.
