---
phase: phase-14
plan: 14-01
type: execute
wave: 1
depends_on: []
must_haves:
  truths:
    - Official LangGraph pathing is required for SDK compatibility.
    - Unauthorized thread access must return a stealth 404.
  artifacts:
    - path: src/schemas/threads.py
      provides: ThreadRunStreamRequest schema.
      min_lines: 20
    - path: src/controllers/threads.py
      provides: POST /threads/{thread_id}/runs/stream endpoint stub.
      min_lines: 30
  key_links:
    - from: Phase 14
      to: REQ-100
      via: Standard endpoint implementation.
files_modified:
  - src/schemas/threads.py
  - src/controllers/threads.py
autonomous: true
requirements:
  - REQ-100: Standard LangGraph Endpoint
---

# 14-01: Standard API Infrastructure

Establish the official LangGraph pathing and request schemas for streaming runs to ensure native SDK compatibility.

## Tasks

<task id="1">
   <name>Define Request Schemas</name>
   <files>
     - src/schemas/threads.py
   </files>
   <action>
     - Add `ThreadRunStreamRequest` to `src/schemas/threads.py`.
     - Fields:
       - `input`: Optional dict (the user message or initial state).
       - `stream_mode`: List[str] (e.g., ["messages", "metadata", "values"]). Default ["messages"].
       - `config`: Optional dict (thread_id etc. - though thread_id is in URL).
       - `checkpoint_id`: Optional str (for specific versions).
   </action>
   <verify>
     - Inspect `src/schemas/threads.py` for Pydantic model correctness.
   </verify>
   <done>
     - [ ] ThreadRunStreamRequest schema defined.
   </done>
</task>

<task id="2">
   <name>Implement Endpoint Stub</name>
   <files>
     - src/controllers/threads.py
   </files>
   <action>
     - Implement `POST /threads/{thread_id}/runs/stream` in `src/controllers/threads.py`.
     - Step 1: Thread ownership validation (Stealth 404).
     - Step 2: Return a `StreamingResponse` using an empty generator for now.
     - Ensure `media_type="text/event-stream"` is set.
   </action>
   <verify>
     - `POST /threads/{id}/runs/stream` returns 200 with `text/event-stream` type via curl/docs.
   </verify>
   <done>
     - [ ] Standard endpoint stub implemented.
   </done>
</task>

## Success Criteria
- API path `/threads/{thread_id}/runs/stream` is accessible and protected by auth.
- Request body correctly validates `stream_mode` and `input`.
- Returns correct SSE media type.
