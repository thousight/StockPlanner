---
phase: 19
plan: 01
title: FMP Migration & Config Purge
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/services/macro.py
  - .env (Instructions only)
autonomous: true
requirements: [FREE-01, CLEAN-01]
---

# Plan 19-01: FMP Migration & Config Purge

**Goal:** Completely remove Finnhub dependencies and migrate the Economic Calendar to Financial Modeling Prep (FMP).

## Wave 1: Config & Service Refactor
<task id="19-01-01" wave="1">
Update `src/config.py`:
- Remove `FINNHUB_API_KEY`.
- Add `FMP_API_KEY`.
</task>

<task id="19-01-02" wave="1">
Refactor `src/services/macro.py`:
- Update `EconomicCalendarService` to use FMP endpoint: `https://financialmodelingprep.com/api/v3/economic_calendar`.
- Ensure it extracts `event`, `date`, `actual`, `estimate`, and `previous`.
- Filter for `country == "US"` and `impact == "High"`.
</task>

<task id="19-01-03" wave="1">
Clean up UAT scripts and mock data:
- Remove any Finnhub-specific logic in `tests/flight_test_phase_18.py`.
- Ensure `_get_mock_calendar_data` remains as a generic fallback.
</task>

## Verification Criteria
- [ ] `EconomicCalendarService` successfully calls FMP (verified via unit test).
- [ ] No mention of Finnhub remains in the core configuration.
