---
phase: 18
plan: 02
title: Dynamic Cache TTL Refactor
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/graph/utils/scraping.py
  - src/graph/utils/news.py
  - src/graph/tools/sec.py
  - src/graph/tools/sentiment.py
autonomous: true
requirements: [DYN-01, CACHE-01]
---

# Plan 18-02: Dynamic Cache TTL Refactor

**Goal:** Refactor the caching mechanism to allow agents to specify the expiration time for research data.

## Wave 2: Utility Refactor
<task id="18-02-01" wave="2">
Update `src/graph/utils/scraping.py`.
- Modify `fetch_content` or wrapper functions to accept `expire_at: Optional[datetime]`.
</task>

<task id="18-02-02" wave="2">
Update `src/graph/utils/news.py`.
- Update `get_summary` to accept `expire_at`.
- Ensure it stores the summary in `ResearchCache` with the provided expiration.
<automated>pytest tests/unit/utils/test_news_util.py</automated>
</task>

<task id="18-02-03" wave="2">
Update `src/graph/tools/sec.py` and `src/graph/tools/sentiment.py`.
- Update `get_sec_filing_section` and `analyze_sentiment` to accept `expire_at`.
- Logic should pass this expiration down to the caching functions.
<automated>pytest tests/unit/tools/test_sentiment.py</automated>
</task>

## Verification Criteria
- [ ] Tools correctly accept and honor an `expire_at` parameter.
- [ ] Database entries in `ResearchCache` reflect the custom expiration times.
- [ ] Cache logic correctly identifies data as "expired" when the custom time passes.
