---
phase: 16
plan: 02
title: Sentiment Engine & Agent Integration
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/graph/tools/sentiment.py
  - src/graph/agents/research/agent.py
  - src/graph/agents/analyst/prompts.py
  - src/graph/agents/summarizer/prompts.py
autonomous: true
requirements: [SENT-01, AGENT-01, UI-01]
---

# Plan 16-02: Sentiment Engine & Agent Integration

**Goal:** Implement the numerical sentiment analysis tool, integrate it into the agentic team, and ensure findings are correctly summarized in the final report.

## Wave 2: Sentiment Tooling
<task id="16-02-01" wave="2">
Implement the `analyze_sentiment(text)` tool in `src/graph/tools/sentiment.py`.
- Use `gpt-4o-mini` with a structured output for `sentiment_score` and `rationale`.
- Implement `get_market_sentiment(ticker)` which aggregates News, SEC, and Social (X/Reddit) data.
- Ensure tool responses are cached in `ResearchCache` to minimize LLM tokens.
<automated>pytest tests/unit/tools/test_sentiment.py</automated>
</task>

## Wave 3: Agentic Integration
<task id="16-02-02" wave="2">
Update the `ResearchAgent` to spawn a dedicated "Social Sentiment" interaction.
- Invoke the new `get_market_sentiment` tool for tickers.
- Differentiate between "News/SEC" (Fundamental) and "Social/X" (Sentiment) outputs.
<automated>pytest tests/integration/test_social_flow.py</automated>
</task>

<task id="16-02-03" wave="2">
Update the `AnalystAgent` and `SummarizerAgent` prompts for weighted sentiment.
- In `AnalystAgent`, update prompts to account for the -1.0 to 1.0 numerical scores.
- In `SummarizerAgent`, ensure a dedicated "Social Sentiment & Market Mood" bubble is present in the final output.
- Implement "Sentiment Shift" detection by comparing current scores to cached historical ones.
<automated>pytest tests/unit/agents/test_logic.py</automated>
</task>

## Verification Criteria
- [ ] `analyze_sentiment` correctly scores a sample positive news headline.
- [ ] `get_market_sentiment` returns a weighted average from multiple sources.
- [ ] Analyst Agent acknowledges a "Sentiment Shift" in its debate output.
- [ ] Final report includes a "Social Sentiment" interaction bubble with a clear mood indicator.
