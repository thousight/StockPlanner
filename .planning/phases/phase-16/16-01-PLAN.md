---
phase: 16
plan: 01
title: Infrastructure & Utility Consolidation
wave: 1
depends_on: []
files_modified:
  - src/database/models.py
  - src/graph/utils/scraping.py
  - src/services/social.py
  - migrations/versions/xxxx_unified_research_cache.py
autonomous: true
requirements: [INFRA-01, SCRAPE-01, XAPI-01]
---

# Plan 16-01: Infrastructure & Utility Consolidation

**Goal:** Consolidate scraping logic into a unified utility, implement the unified `ResearchCache` model, and establish the foundational X API service.

## Wave 1: Database & Utilities
<task id="16-01-01" wave="1">
Implement the `ResearchCache` model in `src/database/models.py` and generate an Alembic migration.
- Fields: `source_type` (Enum), `ticker`, `key` (Unique), `content`, `sentiment_score`, `sentiment_reason`, `expire_at`, `created_at`.
- Migrate existing `NewsCache` data if possible, or just deprecate it.
- Run `alembic revision --autogenerate -m "add unified research cache"`.
<automated>pytest tests/unit/test_crud.py</automated>
</task>

<task id="16-01-02" wave="1">
Create `src/graph/utils/scraping.py` and consolidate fetching logic from `news.py` and `sec.py`.
- Implement `fetch_content(url, user_agent)` with proper error handling and retry logic.
- Implement `clean_html(html)` using `readability-lxml` to extract core content.
- Update `src/graph/tools/news.py` and `src/graph/tools/sec.py` to use these utilities.
<automated>pytest tests/unit/utils/test_scraping.py</automated>
</task>

<task id="16-01-03" wave="1">
Create `src/services/social.py` and implement the `XClient` using Tweepy.
- Implement `get_recent_cashtag_tweets(ticker)` with async support.
- Add mock fallback for development if API keys are missing.
- Ensure proper rate limiting and exception handling.
<automated>pytest tests/unit/services/test_social.py</automated>
</task>

## Verification Criteria
- [ ] `ResearchCache` table exists and replaces `NewsCache`/`SECCache` logic.
- [ ] `fetch_content` correctly uses the passed-down `User-Agent`.
- [ ] `scraping.py` correctly cleans a sample 10-K and news article.
- [ ] `XClient` successfully fetches (or mocks) tweets for a given ticker.
