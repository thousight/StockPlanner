---
phase: phase-13
plan: 13-03
type: execute
wave: 3
depends_on: [13-02]
must_haves:
  truths:
    - Backfill logic must correctly promote missing history from state to relational DB once.
    - No data leakage between users must occur in history APIs.
  artifacts:
    - path: tests/integration/test_history.py
      provides: Comprehensive tests for sync, backfill, and isolation.
      min_lines: 60
  key_links:
    - from: Phase 13
      to: Phase 13 Integration
      via: End-to-end history verification.
files_modified:
  - tests/integration/test_history.py
autonomous: true
requirements:
  - Phase 13 Goal: History Management
---

# 13-03: Memory & History Integration Verification

Validate the end-to-end integrity of the new history management system, including one-time backfill for legacy threads and rigorous multi-user isolation checks.

## Tasks

<task id="1">
   <name>Verify Backfill Logic</name>
   <files>
     - tests/integration/test_history.py
   </files>
   <action>
     - Implement tests for the "on-the-fly" backfill mechanism.
     - Scenario: Create a thread with state in Redis/Postgres checkpointer but NO records in `chat_messages`.
     - Request history for that thread.
     - Verify that the service promotes the state to relational tables during the first load.
   </action>
   <verify>
     - Verify `chat_messages` is populated after the first retrieval request.
   </verify>
   <done>
     - [ ] Backfill logic verified.
   </done>
</task>

<task id="2">
   <name>Verification of Pagination & Deletion</name>
   <files>
     - tests/integration/test_history.py
   </files>
   <action>
     - Implement tests for large conversation histories (e.g., 60+ messages).
     - Verify cursor logic correctly navigates pages without duplicates.
     - Verify individual message deletion and thread deletion hides data correctly.
   </action>
   <verify>
     - All functional tests for retrieval and management pass.
   </verify>
   <done>
     - [ ] Pagination and management verified.
   </done>
</task>

<task id="3">
   <name>Multi-User Isolation Verification</name>
   <files>
     - tests/api/test_isolation.py
   </files>
   <action>
     - Add new isolation tests to `tests/api/test_isolation.py`.
     - Scenario: User B attempts to access User A's history -> 404.
     - Scenario: User B attempts to delete User A's message -> 404.
   </action>
   <verify>
     - `pytest tests/api/test_isolation.py` passes with new cases.
   </verify>
   <done>
     - [ ] History isolation verified.
   </done>
</task>

## Success Criteria
- Existing threads successfully migrate their history to relational storage upon first access.
- Pagination is robust and reliable for deep histories.
- Stealth 404 enforcement is verified for all new history endpoints.
