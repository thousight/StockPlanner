---
phase: phase-10
plan: 10-03
type: execute
wave: 3
depends_on: [10-02]
must_haves:
  truths:
    - Auth dependency must return 401 for expired or invalid tokens.
    - Only users with ACTIVE status can access protected endpoints.
  artifacts:
    - path: src/services/auth.py
      provides: get_current_user FastAPI dependency.
      min_lines: 30
    - path: tests/api/test_auth_api.py
      provides: Integration tests for theft detection and revocation.
      min_lines: 60
  key_links:
    - from: Phase 10
      to: REQ-060
      via: get_current_user middleware implementation.
files_modified:
  - src/services/auth.py
  - tests/api/test_auth_api.py
autonomous: true
requirements:
  - REQ-060: Auth Middleware/Dependency
---

# 10-03: Auth Dependency & Security Verification

Implement the core authentication dependency and verify security features via integration tests.

## Tasks

<task id="1">
   <name>Implement get_current_user Dependency</name>
   <files>
     - src/services/auth.py
   </files>
   <action>
     - Implement `get_current_user(db: AsyncSession, token: str = Depends(OAuth2PasswordBearer))` dependency:
       - Decode JWT and verify signature/expiration.
       - Extract `sub` (user_id).
       - Fetch user from database.
       - Verify user status is `ACTIVE`.
       - Raise `HTTPException(401)` for any failure.
   </action>
   <verify>
     - Create a temporary protected endpoint and verify it returns 401 without a token and 200 with a valid token.
   </verify>
   <done>
     - [ ] Auth dependency implemented.
   </done>
</task>

<task id="2">
   <name>Security Integration Tests</name>
   <files>
     - tests/api/test_auth_api.py
   </files>
   <action>
     - Add tests to `tests/api/test_auth_api.py`:
       - Test successful token refresh.
       - Test theft detection: reuse a rotated token after 10s and verify all sessions are gone.
       - Test sign-out invalidates only one session.
       - Test revoke-all clears all user sessions.
       - Test `get_current_user` with expired and malformed tokens.
   </action>
   <verify>
     - `pytest tests/api/test_auth_api.py` passes.
   </verify>
   <done>
     - [ ] Security verification tests implemented and passing.
   </done>
</task>

## Success Criteria
- All protected endpoints return 401 for unauthorized requests.
- Multi-session handling and theft detection verified via integration tests.
- 100% path coverage for new auth middleware and rotation logic.
