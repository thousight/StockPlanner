---
phase: phase-12
plan: 12-02
type: execute
wave: 2
depends_on: [12-01]
must_haves:
  truths:
    - LangGraph must use Redis for state persistence.
    - Checkpoints must follow a 30-minute sliding window expiration.
  artifacts:
    - path: src/graph/persistence.py
      provides: AsyncRedisSaver initialization with TTL.
      min_lines: 20
    - path: src/controllers/chat.py
      provides: Integration with the new Redis checkpointer.
      min_lines: 50
  key_links:
    - from: Phase 12
      to: REQ-071
      via: AsyncRedisSaver implementation.
files_modified:
  - src/graph/persistence.py
  - src/controllers/chat.py
autonomous: true
requirements:
  - REQ-071: LangGraph Redis Checkpointer
---

# 12-02: LangGraph Redis Checkpointer Migration

Migrate the multi-agent persistence layer from PostgreSQL to Redis and verify sliding-window TTL behavior.

## Tasks

<task id="1">
   <name>Refactor Persistence Layer</name>
   <files>
     - src/graph/persistence.py
   </files>
   <action>
     - Replace `get_checkpointer` logic (previously returning Postgres) with `AsyncRedisSaver`.
     - Configure `AsyncRedisSaver` using `settings.REDIS_URL`.
     - Implement the sliding TTL: `ttl={"default_ttl": settings.REDIS_CHECKPOINT_TTL_MIN, "refresh_on_read": True}`.
   </action>
   <verify>
     - Verify `src/graph/persistence.py` imports and uses `AsyncRedisSaver`.
   </verify>
   <done>
     - [ ] Redis checkpointer implemented.
   </done>
</task>

<task id="2">
   <name>Update Chat Controller</name>
   <files>
     - src/controllers/chat.py
   </files>
   <action>
     - Update the `event_generator` to consume the new `get_checkpointer` (now Redis-backed).
     - Ensure the `thread_id` management remains consistent.
   </action>
   <verify>
     - Run a chat request and verify state persists between messages.
   </verify>
   <done>
     - [ ] Chat controller migrated to Redis persistence.
   </done>
</task>

<task id="3">
   <name>Verify TTL Behavior</name>
   <files>
     - tests/integration/test_redis_persistence.py
   </files>
   <action>
     - Create `tests/integration/test_redis_persistence.py`.
     - Implement a test that saves a checkpoint to Redis and verifies it exists.
     - Implement a test that verifies the TTL is set to 30 minutes.
   </action>
   <verify>
     - `pytest tests/integration/test_redis_persistence.py` passes.
   </verify>
   <done>
     - [ ] Sliding TTL verified.
   </done>
</task>

## Success Criteria
- LangGraph checkpoints are successfully stored in Redis.
- `redis-cli` (or logs) confirms keys expire 30 minutes after last read/write.
- Integration tests confirm no regressions in thread continuity.
