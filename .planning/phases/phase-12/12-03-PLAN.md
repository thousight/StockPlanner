---
phase: phase-12
plan: 12-03
type: execute
wave: 3
depends_on: [12-02]
must_haves:
  truths:
    - Legacy PostgreSQL checkpoint tables must be purged.
    - All code related to PostgreSQL state management must be removed.
  artifacts:
    - path: migrations/versions/
      provides: Migration script to drop checkpoint tables.
      min_lines: 20
  key_links:
    - from: Phase 12
      to: Phase 12 Cleanup
      via: Removal of legacy artifacts.
files_modified:
  - migrations/versions/
  - src/lifecycle/tasks.py
  - main.py
  - requirements.txt
  - migrations/env.py
  - tests/unit/test_lifecycle.py
autonomous: true
requirements:
  - Phase 12 Goal: Cleanup Strategy
---

# 12-03: Legacy Cleanup & Migration

Decommission the old PostgreSQL persistence system and remove associated dead code and filters.

## Tasks

<task id="1">
   <name>Drop Legacy Tables</name>
   <files>
     - migrations/versions/
   </files>
   <action>
     - Create a manual Alembic migration.
     - Add `op.drop_table('checkpoint_writes')`, `op.drop_table('checkpoint_blobs')`, `op.drop_table('checkpoints')`, `op.drop_table('checkpoint_migrations')`.
     - Run `alembic upgrade head`.
   </action>
   <verify>
     - Verify tables are gone from PostgreSQL via DB tool or quick script.
   </verify>
   <done>
     - [ ] Legacy tables dropped.
   </done>
</task>

<task id="2">
   <name>Purge Dead Code & Filters</name>
   <files>
     - src/lifecycle/tasks.py
     - main.py
     - requirements.txt
     - migrations/env.py
     - tests/unit/test_lifecycle.py
   </files>
   <action>
     - Delete `cleanup_checkpoints` from `src/lifecycle/tasks.py`.
     - Remove `cleanup_checkpoints` task registration in `main.py`.
     - Remove `langgraph-checkpoint-postgres` from `requirements.txt`.
     - Remove the `include_object` filter from `migrations/env.py` that was shielding `checkpoint_*` tables.
     - Delete obsolete unit tests for `cleanup_checkpoints` in `tests/unit/test_lifecycle.py`.
   </action>
   <verify>
     - `grep -r "cleanup_checkpoints" .` should yield no results.
     - `grep -r "include_object" migrations/env.py` should yield no results.
     - Application runs without `langgraph-checkpoint-postgres`.
   </verify>
   <done>
     - [ ] Dead code, filters, and dependencies removed.
   </done>
</task>

## Success Criteria
- Database schema is clean of legacy checkpoint tables.
- Codebase is free of PostgreSQL state management logic and migration filters.
- Obsolete tests are removed and full test suite remains green.
- Application remains fully functional with Redis-only persistence.
