# Phase 20: Market Narrative & Sentiment Change Agent

## Step 20-05: Graph Integration & State Update

### Goal
Integrate the `NarrativeResearcher` into the main graph and allow it to share context with other agents.

### Context
The supervisor needs to know when to call the narrative agent, and other agents need access to the broad market context it provides.

### Tasks
- [x] Update `AgentState` in `src/graph/state.py` to include `market_context: NotRequired[str]`.
- [x] Update `Supervisor` agent in `src/graph/graph.py` (via `next_agents.py` and `prompts.py`).
- [x] Update `src/graph/graph.py` structure:
    - [x] Add `NarrativeResearcher` node.
    - [x] Add edges from `Supervisor` to `NarrativeResearcher`.
    - [x] Add logic to store researcher's output in the state.
- [x] Update Analysts to recognize narrative context.

### Verification Criteria
- `Supervisor` correctly routes narrative-related questions to `NarrativeResearcher`.
- `market_context` is populated in the state after the narrative agent runs.
- `Analyst` agent incorporates the narrative context in its final answer.

### Next Step
Step 20-06: Verification & UAT
