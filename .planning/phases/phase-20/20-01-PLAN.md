# Phase 20: Market Narrative & Sentiment Change Agent

## Step 20-01: Schema Migration

### Goal
Add `NARRATIVE` to `ResearchSourceType` and `ReportCategory` enums to support persistent narrative reports and caching.

### Context
Phase 20 requires a dedicated source type and report category for "Market Narratives" to distinguish them from standard ticker news or macro data.

### Tasks
- [x] Update `ReportCategory` enum in `src/database/models.py` to include `NARRATIVE`.
- [x] Update `ResearchSourceType` enum in `src/database/models.py` to include `NARRATIVE`.
- [x] Generate a new Alembic migration: `alembic revision --autogenerate -m "add_narrative_to_enums"`.
- [x] Apply the migration: `alembic upgrade head`.
- [x] Verify the change in the database (e.g., using `check_tables.py` or a SQL client).

### Verification Criteria
- `ReportCategory` contains `NARRATIVE`.
- `ResearchSourceType` contains `NARRATIVE`.
- Migration runs successfully without data loss.
- `ResearchCache` can accept entries with `source_type="NARRATIVE"`.

### Next Step
Step 20-02: Trading Calendar Utilities
