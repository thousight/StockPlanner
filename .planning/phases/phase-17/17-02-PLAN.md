---
phase: 17
plan: 02
title: Graph Topology & Routing Refactor
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/graph/graph.py
  - src/graph/agents/supervisor/agent.py
  - src/graph/agents/supervisor/response.py
  - src/graph/agents/analyst/agent.py
autonomous: true
requirements: [MOD-02, TOP-01, PAR-01]
---

# Plan 17-02: Graph Topology & Routing Refactor

**Goal:** Refactor the graph structure to support parallel fan-out from the Supervisor and fan-in join at the Analyst.

## Wave 2: Supervisor & State
<task id="17-02-01" wave="2">
Update `src/graph/agents/supervisor/response.py`.
- Change `next_agent` from a single string to a list of strings (`next_agents: List[str]`) to support parallel fan-out.
<automated>pytest tests/unit/agents/test_supervisor_node.py</automated>
</task>

<task id="17-02-02" wave="2">
Update `src/graph/agents/supervisor/agent.py`.
- Refactor `supervisor_agent` to use the LangGraph `Command` (or return `Send` list) to trigger multiple researchers in parallel.
- Update the logical loop detection to account for parallel branches.
</task>

## Wave 3: Graph Topology
<task id="17-02-03" wave="3">
Update `src/graph/graph.py`.
- Add nodes for `fundamental_research`, `sentiment_research`, and `macro_research`.
- Configure conditional edges from `supervisor` to use the `Send` pattern based on the supervisor's output.
- Configure all research nodes to flow into `analyst` (implicit fan-in).
- Update `MeshRouter` or use direct edges where possible to simplify the mesh.
<automated>pytest tests/integration/test_parallel_graph.py</automated>
</task>

## Verification Criteria
- [ ] Supervisor can output multiple next agents.
- [ ] Graph successfully executes researchers in parallel (verified via logs).
- [ ] Analyst waits for all active researchers to finish before starting.
