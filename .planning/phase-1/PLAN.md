---
phase: 01-multi-agent-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/state.py
  - src/agents/graph.py
  - src/agents/nodes/supervisor/node.py
  - src/agents/nodes/supervisor/prompts.py
  - src/agents/nodes/research/node.py
  - src/agents/nodes/research/prompts.py
  - src/agents/nodes/analyst/node.py
  - src/agents/nodes/analyst/prompts.py
  - src/agents/nodes/analyst/subgraph.py
  - src/agents/utils/news.py
  - src/database/models.py
  - src/database/crud.py
  - src/web/app.py
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, RES-01, RES-02, ANL-01, ANL-02, CACHE-01, UI-01, UI-02]
must_haves:
  truths:
    - "Supervisor generates a structured HighLevelPlan before delegating."
    - "Research agent creates a ResearchPlan before fetching data."
    - "Analyst executes a parallel Bull vs. Bear debate."
    - "News summaries are cached for 7 days."
    - "Stock analysis is cached until 6pm/6am."
    - "UI displays the plan and the debate details."
  artifacts:
    - path: "src/agents/nodes/supervisor/node.py"
      provides: "Supervisor planning logic"
    - path: "src/agents/nodes/research/node.py"
      provides: "Research local planning"
    - path: "src/agents/nodes/analyst/subgraph.py"
      provides: "Analyst debate subgraph"
    - path: "src/database/models.py"
      contains: "class AnalysisCache"
  key_links:
    - from: "src/agents/graph.py"
      to: "src/agents/nodes/supervisor/node.py"
      via: "graph node"
    - from: "src/agents/nodes/analyst/node.py"
      to: "src/agents/nodes/analyst/subgraph.py"
      via: "subgraph invocation"
---

<objective>
Refactor Phase 1 implementation to establish a "Plan-and-Execute" mesh architecture with a Supervisor Planner, Intelligent Research Agent, and Adversarial Analyst Subgraph.

Purpose: Move beyond simple routing to a sophisticated multi-agent system capable of complex reasoning and self-correction.
Output: A fully refactored agent graph with planning, debate, and caching capabilities.
</objective>

<execution_context>
@.planning/phase-1/1-CONTEXT.md
@.planning/phase-1/RESEARCH.md
</execution_context>

<context>
@src/agents/graph.py
@src/agents/state.py
@src/agents/nodes/supervisor/node.py
@src/agents/nodes/research/utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core State & Supervisor Planning</name>
  <files>src/agents/state.py, src/agents/nodes/supervisor/node.py, src/agents/nodes/supervisor/prompts.py</files>
  <action>
    1. Update `AgentState` in `state.py` to include:
       - `high_level_plan`: List[str] (The steps generated by Supervisor)
       - `research_plan`: Dict[str, Any] (The specific plan from Research Agent)
       - `debate_output`: Dict[str, Any] (The Bull/Bear arguments)
       - `analysis_cache_key`: str (For caching logic)
    2. Refactor `supervisor_node` in `node.py`:
       - Instead of direct routing, use `ChatOpenAI` with a structured output (Pydantic) to generate a `HighLevelPlan`.
       - The plan should decompose the user request into steps (e.g., "Research Ticker X", "Analyze Financials", "Synthesize Report").
       - Update `SUPERVISOR_PROMPT` in `prompts.py` to support this decomposition.
       - The node should return `{"high_level_plan": plan, "next_node": first_step_node}`.
  </action>
  <verify>
    Run a test script invoking `supervisor_node` with a sample state. Verify `high_level_plan` is populated in the return value.
  </verify>
  <done>
    `AgentState` has new fields. `supervisor_node` returns a structured plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Research Agent & News Utility Refactor</name>
  <files>src/agents/nodes/research/node.py, src/agents/nodes/research/prompts.py, src/agents/utils/news.py, src/database/models.py, src/database/crud.py</files>
  <action>
    1. Create `src/agents/utils/news.py`:
       - Move `get_stock_news`, `get_macro_economic_news`, `get_summary` from `research/utils.py` here.
       - Update `get_summary` to use `NewsCache` with a 7-day TTL (update `save_cache` in `crud.py`).
    2. Refactor `research_node` in `node.py`:
       - Implement "Local Planning": Before fetching, generate a `ResearchPlan` (e.g., "Need 10-K for AAPL", "Need recent news on AI").
       - Use the `NewsUtility` and `yfinance` tools to execute the plan.
       - Update `prompts.py` with `RESEARCH_PLANNER_PROMPT`.
    3. Update `models.py`:
       - Ensure `NewsCache` table exists and supports the TTL logic.
  </action>
  <verify>
    Run `research_node` with a target. Verify it generates a plan (print it) and then executes it, caching news summaries.
  </verify>
  <done>
    Research agent performs local planning. News is a utility with 7-day caching.
  </done>
</task>

<task type="auto">
  <name>Task 3: Analyst Debate Subgraph</name>
  <files>src/agents/nodes/analyst/node.py, src/agents/nodes/analyst/subgraph.py, src/agents/nodes/analyst/prompts.py</files>
  <action>
    1. Create `subgraph.py`:
       - Define `AnalystState` (subset of AgentState).
       - Create nodes: `generate_instructions`, `bull_agent`, `bear_agent`, `synthesizer`.
       - Use `langgraph.graph.StateGraph` to build the subgraph.
       - Parallelize `bull_agent` and `bear_agent`.
    2. Create `prompts.py`:
       - `INSTRUCTION_GENERATOR_PROMPT`: Analyzes research data to give specific angles to Bull/Bear.
       - `BULL_PROMPT` / `BEAR_PROMPT`: Adversarial personas.
       - `SYNTHESIS_PROMPT`: Point-counterpoint evaluation.
    3. Refactor `analyst_node` in `node.py` to invoke this subgraph.
  </action>
  <verify>
    Run `analyst_node` with dummy research data. Verify it produces a Bull argument, Bear argument, and final synthesis.
  </verify>
  <done>
    Analyst executes a parallel debate. Output contains `bull_analysis`, `bear_analysis`, and `final_report`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Caching & UI Visualization</name>
  <files>src/database/models.py, src/database/crud.py, src/agents/nodes/nodes.py, src/web/app.py</files>
  <action>
    1. Update `models.py`:
       - Add `AnalysisCache` table (ticker, report, debate_summary, expires_at).
    2. Update `crud.py`:
       - Add `get_analysis_cache` and `save_analysis_cache`.
       - Implement 6pm/6am TTL logic:
         - Weekdays: Expires next 6:00 AM or 6:00 PM (whichever is sooner + buffer).
         - Weekends: Expires Monday 6:00 AM.
    3. Update `nodes.py` (or `analyst/node.py`):
       - Check cache before running Analyst subgraph.
    4. Update `src/web/app.py`:
       - Use `st.status` to show the Supervisor's `high_level_plan`.
       - Use `st.expander` to show `bull_analysis` and `bear_analysis` side-by-side (or sequential).
  </action>
  <verify>
    Run the full graph. specific checks:
    - Run twice for same ticker -> second run should hit cache.
    - Check UI for expandable debate sections.
  </verify>
  <done>
    Analysis is cached with smart TTL. UI visualizes the plan and the debate.
  </done>
</task>

</tasks>

<verification>
- [ ] Run `python tests/test_analyst_node.py` (needs update) to verify debate logic.
- [ ] Run `streamlit run src/web/app.py` and verify the new visualization.
- [ ] Check SQLite database for `news_cache` and `analysis_cache` entries.
</verification>

<success_criteria>
- Supervisor decomposes requests into a plan.
- Research agent plans before executing.
- Analyst agent produces Bull/Bear arguments.
- News is cached for 7 days.
- UI shows the "fight".
</success_criteria>

<output>
After completion, update `ROADMAP.md` to mark Phase 1 Refactor items as done.
</output>
